import { Meta, ColorPalette, ColorItem } from '@storybook/addon-docs/blocks';
import {lightTheme, darkTheme} from '../components/ui/theme/colors';
 
<Meta title="Colors" />
 
<h2>Light Theme</h2>
<ColorPalette key={name}>
  {Object.entries(lightTheme).map(([name, value]) => (
  <ColorItem
    key={name}
    title={name}
    subtitle={value.description}
    colors={{ [`var(${name})`]: value }}
  />
))}
</ColorPalette>
<h2>Dark Theme</h2>
<ColorPalette key={name}>
{Object.entries(darkTheme).map(([name, value]) => (
  <ColorItem
    key={name}
    title={name}
    subtitle={value.description}
    colors={{ [`var(${name})`]: value }}
  />
))}
</ColorPalette>